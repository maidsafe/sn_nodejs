{
  "name": "sn_api",
  "napi": {
    "name": "sn_api"
  },
  "main": "sn_api.node",
  "types": "./sn_api.d.ts",
  "devDependencies": {
    "@napi-rs/cli": "^1.0.0",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.1",
    "typescript": "^4.1.5"
  },
  "engines": {
    "node": ">= 10"
  },
  "scripts": {
    "build": "napi build",
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "./tests-js/environment-safe",
    "testTimeout": 500000
  }
}